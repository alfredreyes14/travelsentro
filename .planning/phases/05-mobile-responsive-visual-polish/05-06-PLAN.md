---
phase: 05-mobile-responsive-visual-polish
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - components/ui/dialog.tsx
  - components/ui/alert-dialog.tsx
autonomous: true
gap_closure: true
requirements: []
must_haves:
  truths:
    - "Cards, dialogs, and dropdowns show a consistent shadow/elevation hierarchy, within the locked brand system (roadmap Phase 5 SC5) -- closing 05-VERIFICATION.md's failed truth #5"
    - "DialogContent (dialog.tsx) and AlertDialogContent (alert-dialog.tsx) -- the floating/modal surfaces behind message compose, add/edit account, and delete confirmations across the admin panel -- now visually sit above the static, in-flow Card (shadow-sm, added in 05-05-PLAN.md) rather than below it"
  artifacts:
    - path: "components/ui/dialog.tsx"
      expected: "DialogContent's className (line 56) includes shadow-md alongside the existing ring-1 ring-foreground/10"
    - path: "components/ui/alert-dialog.tsx"
      expected: "AlertDialogContent's className (line 55) includes shadow-md alongside the existing ring-1 ring-foreground/10"
  key_links:
    - "DialogContent's/AlertDialogContent's added shadow-md -> the same Tailwind default shadow-md value already used by dropdown-menu.tsx's DropdownMenuContent (line 44) and select.tsx's SelectContent (line 86) for floating surfaces -- matches an existing convention, no new elevation value invented"
    - "Dialog/AlertDialog's shadow-md sits one step above Card's shadow-sm (components/ui/card.tsx, added in 05-05-PLAN.md) -- restores the intended elevation hierarchy: static Card (shadow-sm) < floating Dialog/AlertDialog/DropdownMenu/Select (shadow-md/shadow-lg)"
---

<objective>
Close 05-VERIFICATION.md's single code-level gap: `DialogContent` (`components/ui/dialog.tsx`) and `AlertDialogContent` (`components/ui/alert-dialog.tsx`) currently carry zero `shadow-*` utility classes (only `ring-1 ring-foreground/10`), while `components/ui/card.tsx` gained `shadow-sm` in `05-05-PLAN.md` and `components/ui/dropdown-menu.tsx`/`components/ui/select.tsx` already carry `shadow-md`/`shadow-lg`. This inverts the intended elevation hierarchy -- the static, in-flow `Card` now reads as more elevated than the floating, modal `Dialog`/`AlertDialog` surfaces used for message compose, add/edit account, and delete confirmations throughout the admin panel.

Purpose: `05-RESEARCH.md`'s Pitfall 4 incorrectly asserted that `dialog.tsx` already used `shadow-md`/`shadow-lg` alongside `dropdown-menu.tsx`/`select.tsx`, which is why `05-05-PLAN.md`'s Task 2 scoped shadow work to `Card` only. This plan corrects that gap directly per D-06/D-07's already-locked ring-vs-shadow elevation convention (not new design territory) -- a narrow, mechanical two-file className addition.
Output: `components/ui/dialog.tsx` and `components/ui/alert-dialog.tsx` with `shadow-md` added to their respective content components, restoring roadmap SC5's "consistent shadow/elevation hierarchy" across cards, dialogs, and dropdowns.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-mobile-responsive-visual-polish/05-CONTEXT.md
@.planning/phases/05-mobile-responsive-visual-polish/05-VERIFICATION.md
</context>

<artifacts_produced>
No new files, components, hooks, types, or Server Actions. This plan modifies two existing shared UI primitives in place -- only the `className` strings of `DialogContent` (dialog.tsx) and `AlertDialogContent` (alert-dialog.tsx) change. Every other export from both files is untouched.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Add shadow-md to DialogContent</name>
  <files>components/ui/dialog.tsx</files>
  <read_first>
    components/ui/dialog.tsx (full file -- DialogContent's className string at line 56, currently ending in `...text-popover-foreground ring-1 ring-foreground/10 duration-100 outline-none sm:max-w-sm...` with no shadow utility anywhere in the file)
    components/ui/dropdown-menu.tsx line 44 (DropdownMenuContent's className includes `shadow-md ring-1 ring-foreground/10` -- the floating-surface elevation convention to match)
    components/ui/select.tsx line 86 (SelectContent's className includes `shadow-md ring-1 ring-foreground/10` -- second confirming reference for the same convention)
    components/ui/card.tsx line 15 (Card's className already includes `ring-1 ring-foreground/10 shadow-sm`, added in 05-05-PLAN.md -- the elevation step this task must sit visually above)
  </read_first>
  <action>
    In components/ui/dialog.tsx's DialogContent function, insert the shadow-md utility class into the className string on line 56, placed immediately adjacent to the existing `ring-1 ring-foreground/10` segment (do not remove or reorder any other class in the string). Use exactly `shadow-md` -- the identical value already present in dropdown-menu.tsx's DropdownMenuContent and select.tsx's SelectContent -- so Dialog shares the same floating-surface elevation step as those two components rather than introducing a distinct value. Leave DialogOverlay, DialogPortal, DialogClose, DialogHeader, DialogFooter, DialogTitle, and DialogDescription completely unmodified; this task touches only the one className string on line 56.
  </action>
  <acceptance_criteria>
    - `grep -c 'shadow-md' components/ui/dialog.tsx` is >= 1.
    - `grep -c 'ring-1 ring-foreground/10' components/ui/dialog.tsx` is >= 1 (ring preserved, not replaced).
    - `git diff components/ui/dialog.tsx` shows changes confined to the single DialogContent className string (line 56) -- no other line touched.
    - `npm run build` completes with no new TypeScript errors.
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'shadow-md' components/ui/dialog.tsx; grep -c 'ring-1 ring-foreground/10' components/ui/dialog.tsx; npm run build</automated>
  </verify>
  <done>DialogContent's className includes shadow-md alongside its existing ring-1 ring-foreground/10, matching dropdown-menu.tsx/select.tsx's floating-surface elevation convention; every other Dialog subcomponent is byte-identical to before this task.</done>
</task>

<task type="auto">
  <name>Task 2: Add shadow-md to AlertDialogContent, verify the restored elevation hierarchy</name>
  <files>components/ui/alert-dialog.tsx</files>
  <read_first>
    components/ui/alert-dialog.tsx (full file -- AlertDialogContent's className string at line 55, currently ending in `...text-popover-foreground ring-1 ring-foreground/10 duration-100 outline-none data-[size=default]:max-w-xs...` with no shadow utility anywhere in the file)
    components/ui/dialog.tsx (post-Task-1 state -- confirms the identical shadow-md value now used for DialogContent, so both modal primitives share one elevation step)
    components/ui/dropdown-menu.tsx line 44 and components/ui/select.tsx line 86 (shadow-md reference, same as Task 1)
    05-VERIFICATION.md lines 8-18 (the `gaps:` frontmatter block this task closes)
  </read_first>
  <action>
    In components/ui/alert-dialog.tsx's AlertDialogContent function, insert the shadow-md utility class into the className string on line 55, placed immediately adjacent to the existing `ring-1 ring-foreground/10` segment (do not remove or reorder any other class in the string, including the `group/alert-dialog-content` and `data-[size=...]` prefixes/suffixes). Use exactly `shadow-md`, matching Task 1's DialogContent addition and dropdown-menu.tsx/select.tsx's existing convention. Leave AlertDialogOverlay, AlertDialogPortal, AlertDialogHeader, AlertDialogFooter, AlertDialogMedia, AlertDialogTitle, AlertDialogDescription, AlertDialogAction, and AlertDialogCancel completely unmodified. After both tasks, run a combined check confirming the full elevation scale now reads consistently: Card (shadow-sm) sits below Dialog/AlertDialog/DropdownMenu/Select (shadow-md/shadow-lg), with the ring-1 ring-foreground/10 border preserved on every one of the four components.
  </action>
  <acceptance_criteria>
    - `grep -c 'shadow-md' components/ui/alert-dialog.tsx` is >= 1.
    - `grep -c 'ring-1 ring-foreground/10' components/ui/alert-dialog.tsx` is >= 1 (ring preserved, not replaced).
    - `git diff components/ui/alert-dialog.tsx` shows changes confined to the single AlertDialogContent className string (line 55) -- no other line touched.
    - `grep -c 'shadow-sm' components/ui/card.tsx` is still >= 1 (unaffected by this plan, confirms the elevation floor is unchanged).
    - `npm run build` completes with no new TypeScript errors.
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'shadow-md' components/ui/alert-dialog.tsx; grep -c 'ring-1 ring-foreground/10' components/ui/alert-dialog.tsx; grep -c 'shadow-sm' components/ui/card.tsx; npm run build</automated>
  </verify>
  <done>AlertDialogContent's className includes shadow-md alongside its existing ring-1 ring-foreground/10; Dialog and AlertDialog now share the identical floating-surface elevation convention as dropdown-menu.tsx/select.tsx, sitting visibly above Card's shadow-sm -- closing 05-VERIFICATION.md's gap and restoring roadmap SC5's intended shadow/elevation hierarchy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | Pure CSS/className changes on two already-gated, already-shipped shared UI primitives (DialogContent, AlertDialogContent); no new input handling, no new data path, no new dependency introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-------------------|
| T-05-09 | — | dialog.tsx, alert-dialog.tsx | low | accept | No new attack surface -- both changes add a single Tailwind default `shadow-md` utility already used elsewhere in this codebase (dropdown-menu.tsx, select.tsx). `DialogPrimitive`/`AlertDialogPrimitive`'s underlying `@base-ui/react` logic, all Close/Overlay/Portal behavior, and every consumer's data/permission flow (message compose, add/edit account, delete confirmations) are untouched -- verified via each task's `git diff` acceptance criterion confirming no line beyond the single className string changed. |

</threat_model>

<verification>
- `npm run build` succeeds after both tasks.
- `grep -c 'shadow-md' components/ui/dialog.tsx` and `grep -c 'shadow-md' components/ui/alert-dialog.tsx` are each >= 1.
- `grep -c 'ring-1 ring-foreground/10'` remains >= 1 in both files (preserved, not replaced).
- `git diff components/ui/dialog.tsx components/ui/alert-dialog.tsx` shows only the two targeted className strings changed -- no other subcomponent touched.
- The full elevation scale is now consistent: `components/ui/card.tsx` (`shadow-sm`) < `components/ui/dialog.tsx`/`components/ui/alert-dialog.tsx`/`components/ui/dropdown-menu.tsx`/`components/ui/select.tsx` (`shadow-md`/`shadow-lg`), all still carrying `ring-1 ring-foreground/10`.
</verification>

<success_criteria>
Dialog and AlertDialog surfaces now carry `shadow-md`, matching `dropdown-menu.tsx`/`select.tsx`'s existing floating-surface convention and sitting visually above `Card`'s `shadow-sm` -- closing 05-VERIFICATION.md's gap and fully satisfying roadmap Phase 5 SC5's "cards/dialogs/dropdowns show a consistent shadow/elevation hierarchy."
</success_criteria>

<output>
Create `.planning/phases/05-mobile-responsive-visual-polish/05-06-SUMMARY.md` when done
</output>
