---
phase: 05-mobile-responsive-visual-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - components/admin/crm-table.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Below the md (768px) breakpoint, /admin/crm renders contacts as stacked cards instead of a raw table or horizontal-scroll table (D-03)"
    - "Bulk row-selection and the 'Message Selected' bulk-send flow (MSG-03/MSG-04) work identically in the mobile card layout, including the opted-out disabled-checkbox guard (D-04)"
    - "Card-mode rows show the same hover/active visual feedback as desktop table rows, and the nested checkbox keeps its existing focus-visible ring (D-07)"
  artifacts:
    - "components/admin/crm-table.tsx — new md:hidden Card-per-row branch alongside the existing hidden md:block Table branch"
  key_links:
    - "Card-mode Checkbox -> the SAME row.getIsSelected()/row.toggleSelected()/opted_out branch already used by the table's select column cell renderer — never a parallel useState-based selection"
    - "Card-mode card tap -> the same router.push(`/admin/crm/${row.original.id}`) navigation as the desktop TableRow onClick"
---

<objective>
Retrofit `components/admin/crm-table.tsx` so contacts render as a stacked-card layout below 768px (D-03), with bulk row-selection, the opted-out consent guard, and the "Message Selected" bulk-send entry point preserved exactly as they work on desktop (D-04).

Purpose: `crm-table.tsx` is the only true `@tanstack/react-table` instance in the admin panel, with `enableRowSelection` gated on `!row.original.opted_out`. This selection state and consent guard directly back MSG-03/MSG-04's bulk email/SMS flow — a re-implementation in card mode risks silently bypassing the opt-out guard (PH Data Privacy Act / provider-suspension risk already flagged in STATE.md blockers), so the card branch must reuse the table's own selection state and cell renderer, not duplicate it.
Output: `crm-table.tsx` with a CSS-only twin-markup table/card breakpoint switch, functionally verified.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-mobile-responsive-visual-polish/05-CONTEXT.md
@.planning/phases/05-mobile-responsive-visual-polish/05-RESEARCH.md
@.planning/phases/05-mobile-responsive-visual-polish/05-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add CSS-only card-mode branch to crm-table.tsx</name>
  <files>components/admin/crm-table.tsx</files>
  <read_first>
    components/admin/crm-table.tsx (full file — columns array incl. the "select" column's opted_out-guarded cell renderer at lines 65-98, the existing Table render at lines 262-296, the bulk action bar at 234-247, the empty-match state at 249-260)
    components/ui/card.tsx (Card/CardContent exports)
    05-RESEARCH.md Pattern 1 (twin-markup) and Pitfall 2 (why NOT to use useIsMobile() for this switch)
    05-PATTERNS.md's crm-table.tsx section (row-selection/opt-out guard code to preserve verbatim)
  </read_first>
  <action>
    Import `Card` from "@/components/ui/card" into components/admin/crm-table.tsx. Wrap the existing `<Table>` render (the block starting `<div className="overflow-hidden rounded-xl border border-border">`) in an additional `hidden md:block` wrapper so it only renders at md and above — internals unchanged. Add a new sibling `<div className="flex flex-col gap-3 md:hidden">` that maps over `rows` (from `table.getRowModel().rows`) and renders one `<Card key={row.id} className="cursor-pointer p-4 transition-colors hover:bg-muted/50 active:bg-muted" onClick={() => router.push(\`/admin/crm/${row.original.id}\`)}>` per row containing: (a) the select cell's output obtained via `flexRender(row.getVisibleCells().find((c) => c.column.id === "select")!.column.columnDef.cell, row.getVisibleCells().find((c) => c.column.id === "select")!.getContext())` — i.e. render the EXISTING "select" column's cell definition through flexRender rather than re-authoring the opted_out/Tooltip/Checkbox JSX a second time, so there is exactly one implementation of the consent guard; (b) `row.original.name` and `row.original.email` as primary/secondary text; (c) the status Badge using the same `STATUS_BADGE_VARIANT`/`STATUS_BADGE_CLASSNAME`/`STATUS_LABELS` lookups already imported; (d) `row.original.tags` mapped to outline Badges plus the opted-out Badge, matching the existing "tags" column cell exactly. Do not import or call `useIsMobile()` anywhere in this file — the breakpoint switch must be pure CSS (`hidden md:block` / `md:hidden`), per RESEARCH.md Pitfall 2, to avoid an SSR/hydration flash. Do not add a new `useState` for selection — the card branch reads/writes the exact same `rowSelection` state via `row.getIsSelected()`/`row.toggleSelected()` (already true if you reuse the select column's cell via flexRender). The bulk action bar (`{selectedRows.length > 0 ? ...}`) and `MessageComposeDialog` stay outside/above both branches, completely unchanged — they already read `table.getSelectedRowModel().rows`, which is breakpoint-agnostic.

    D-07 hover/focus/active-state coverage: the card's `hover:bg-muted/50 active:bg-muted` pair mirrors the desktop TableRow's existing `cursor-pointer hover:bg-muted/50` tap/hover feedback (crm-table.tsx's own Table branch), so mobile card mode gets the same visual interaction cue as desktop, using the identical Tailwind utility already present in this file — not a new token. Do not add `tabIndex`/`role="button"`/keyboard handling to the Card itself: like its desktop TableRow counterpart (which is also a mouse/touch-only `onClick` target with no keyboard support), the card is not meant to become independently keyboard-focusable in this phase — that would be a pre-existing accessibility gap unrelated to D-07's visual-polish scope, not something newly introduced here. Focus-visible coverage for this row's one keyboard-operable control is already satisfied structurally: the nested Checkbox (rendered via the reused "select" column cell) carries its own `focus-visible:ring-3 focus-visible:ring-ring/50` from `components/ui/checkbox.tsx`'s existing base styles, with zero changes needed.
  </action>
  <acceptance_criteria>
    - `grep -c 'md:hidden' components/admin/crm-table.tsx` is >= 1 and `grep -c 'hidden md:block' components/admin/crm-table.tsx` is >= 1.
    - `grep -c 'useIsMobile' components/admin/crm-table.tsx` is exactly 0.
    - The new card branch calls `flexRender` on the "select" column's own `columnDef.cell` (or equivalently reuses `row.getIsSelected()`/`row.toggleSelected()` directly) rather than authoring a second, separate `Checkbox`/opted_out conditional.
    - `grep -c 'hover:bg-muted/50' components/admin/crm-table.tsx` is >= 2 (existing desktop TableRow's hover class + the new card's hover class), satisfying D-07's hover/active interactive-state requirement for the card row.
    - `npm run build` completes with no new TypeScript errors.
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'md:hidden' components/admin/crm-table.tsx; grep -c 'hidden md:block' components/admin/crm-table.tsx; grep -c 'useIsMobile' components/admin/crm-table.tsx; grep -c 'hover:bg-muted/50' components/admin/crm-table.tsx; npm run build</automated>
  </verify>
  <done>Below 768px, /admin/crm renders contacts as Cards with a working checkbox (respecting the opted_out guard), status badge, tags, tap-to-navigate, and hover/active feedback matching desktop's TableRow (D-07); at/above 768px, the existing Table renders exactly as before.</done>
</task>

<task type="auto">
  <name>Task 2: Live verification of CRM card-mode selection, bulk send, and empty state</name>
  <files>components/admin/crm-table.tsx</files>
  <read_first>
    components/admin/message-compose-dialog.tsx (confirms bulk-mode props are unaffected by this change)
    The Task 1 diff on components/admin/crm-table.tsx
  </read_first>
  <action>
    Run `npm run dev` and open /admin/crm with seeded contacts that include at least one opted-out contact and enough contacts to trigger the search filter's empty state. Resize the browser (or use DevTools' device toolbar) to below 768px width and confirm each of: (1) Cards render instead of the Table; (2) selecting 2+ non-opted-out contacts shows the "N selected" action bar and clicking "Message Selected" opens `MessageComposeDialog` in bulk mode with exactly the selected contacts; (3) the opted-out contact's checkbox renders disabled with the same "Opted out — excluded from bulk sends" tooltip copy as desktop; (4) tapping a card outside the checkbox area navigates to `/admin/crm/[id]`, while tapping the checkbox itself does not navigate (stopPropagation preserved); (5) typing a search term that matches zero contacts still renders the single existing "No contacts match your search" empty state (not duplicated once per breakpoint, since that block sits outside both the table and card branches).
  </action>
  <acceptance_criteria>
    - All 5 numbered confirmations in the action pass with no console errors during the interaction.
  </acceptance_criteria>
  <verify>
    <automated>npm run lint</automated>
    <human-check>At a &lt;768px viewport on /admin/crm, verify all 5 points in the action (card rendering, bulk-select + Message Selected, opted-out disabled checkbox + tooltip, tap-to-navigate excluding the checkbox, single non-duplicated empty state).</human-check>
  </verify>
  <done>Card-mode selection, bulk compose trigger, opt-out guard, card-tap navigation, and the empty state all behave identically to desktop's row-mode equivalents.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Admin browser -> sendBulkEmail/sendBulkSms Server Actions | Card-mode UI is a new rendering path into the same existing, permission-gated (`can_message_customers`) bulk-send Server Actions from Phase 4 — this plan does not touch those Server Actions themselves. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-------------------|
| T-05-02 | Tampering | crm-table.tsx card-mode select cell | high | mitigate | Card mode MUST render the existing "select" column's cell via `flexRender`/`row.getIsSelected()`/`row.toggleSelected()` rather than re-implementing the opted_out check — verified via Task 1's grep gate (no second Checkbox/opted_out conditional) and Task 2's live confirmation that the opted-out contact stays unselectable in card mode. A re-implemented, drifted guard would let opted-out contacts receive bulk messages, violating MSG-05 and PH Data Privacy Act exposure already flagged in STATE.md blockers. |
| T-05-03 | Elevation of Privilege | crm-table.tsx card-mode navigation/actions | low | accept | Card mode calls the identical `router.push`/`MessageComposeDialog` entry points already reached from desktop rows — no new Server Action call site is introduced, so AUTH-05's server-side enforcement is untouched. |

</threat_model>

<verification>
- `npm run build` and `npm run lint` pass.
- Live check confirms card-mode selection/bulk-send/opt-out/navigation/empty-state parity with desktop.
</verification>

<success_criteria>
- crm-table.tsx renders cards below 768px and the existing table at/above it, with zero duplicated selection logic and zero new Server Action call sites.
</success_criteria>

<output>
Create `.planning/phases/05-mobile-responsive-visual-polish/05-02-SUMMARY.md` when done
</output>
