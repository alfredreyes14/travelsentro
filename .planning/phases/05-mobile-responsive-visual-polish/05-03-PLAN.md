---
phase: 05-mobile-responsive-visual-polish
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - components/admin/sortable-package-list.tsx
  - components/admin/package-list-row.tsx
  - components/admin/package-list-card.tsx
  - app/admin/(dashboard)/packages/loading.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Below the md (768px) breakpoint, /admin/packages renders packages as stacked, drag-reorderable cards instead of a raw table (D-03)"
    - "Navigating to /admin/packages shows a skeleton placeholder instead of a blank flash while the initial Supabase query resolves (D-07)"
    - "The card's drag-handle button shows hover/focus-visible/active feedback, and Switch/DropdownMenu controls keep their existing shadcn hover/focus/active defaults (D-07)"
    - "The existing page-level 'No packages yet' empty state (D-07) already covers card mode since it renders in place of SortablePackageList regardless of breakpoint — confirmed, not rebuilt"
  artifacts:
    - "components/admin/package-list-card.tsx — new PackageListCard component"
    - "app/admin/(dashboard)/packages/loading.tsx — new PackagesLoading route-level Suspense fallback"
  key_links:
    - "PackageListCard's useSortable({ id: item.id }) -> the SAME DndContext/SortableContext pair already wrapping PackageListRow, not a second independent drag implementation"
    - "PackageListCard's Published/Featured Switch handlers -> the SAME publishPackage/featurePackage/softDeletePackage Server Actions already called by PackageListRow, no new call sites"
---

<objective>
Retrofit the packages admin list so packages render as stacked, drag-reorderable cards below 768px (D-03), and add the missing `packages/loading.tsx` skeleton (D-07's confirmed empty-state gap).

Purpose: `sortable-package-list.tsx` drives a `@dnd-kit/sortable` list (not TanStack Table) rendering `PackageListRow` inside `<TableBody>`. `useSortable()`'s returned props/refs are markup-agnostic, so the same hook call site can attach to a `<Card>` instead of a `<TableRow>` without touching drag logic (confirmed in RESEARCH.md). `/admin/packages` currently has no `loading.tsx` (unlike `/admin/crm`), causing a blank-white flash on load.
Output: a new `PackageListCard` sibling component, a card-mode branch wired into `sortable-package-list.tsx`, and `app/admin/(dashboard)/packages/loading.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-mobile-responsive-visual-polish/05-CONTEXT.md
@.planning/phases/05-mobile-responsive-visual-polish/05-RESEARCH.md
@.planning/phases/05-mobile-responsive-visual-polish/05-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract PackageListCard + wire card-mode branch into SortablePackageList</name>
  <files>components/admin/package-list-card.tsx, components/admin/sortable-package-list.tsx, components/admin/package-list-row.tsx</files>
  <read_first>
    components/admin/sortable-package-list.tsx (full file — DndContext/SortableContext wrapping TableBody, handleDragEnd, handleMutated, handleDeleted)
    components/admin/package-list-row.tsx (full file — useSortable() wiring, handlePublishChange/handleFeatureChange/handleDelete, DropdownMenu/AlertDialog delete confirmation)
    components/ui/card.tsx, components/ui/switch.tsx
    05-RESEARCH.md Pattern 2 (useSortable on a Card) and 05-PATTERNS.md's sortable-package-list.tsx/package-list-row.tsx section
  </read_first>
  <action>
    Create `components/admin/package-list-card.tsx` exporting `function PackageListCard({ item, onMutated, onDeleted }: { item: AdminPackageListItem; onMutated: () => void; onDeleted: (id: string) => void })` (import `AdminPackageListItem` from "@/app/admin/(dashboard)/packages/page", matching package-list-row.tsx's existing import). Copy verbatim from package-list-row.tsx: the `useSortable({ id: item.id })` destructure, the `style` object (`CSS.Transform.toString(transform)`/`transition`/`opacity: isDragging ? 0.6 : 1`), the `isPublished`/`isFeatured`/`isDeleteOpen`/`isPending` state, and the `handlePublishChange`/`handleFeatureChange`/`handleDelete` functions — these must call the identical `publishPackage`/`featurePackage`/`softDeletePackage` imports from "@/actions/packages", not a new wrapper. Render `<Card ref={setNodeRef} style={style} className="p-4">` containing: a drag-handle button reusing the exact `{...attributes} {...listeners}` Tooltip pattern (GripVerticalIcon, "Drag to reorder"), a `relative size-12 overflow-hidden rounded-md bg-secondary/10` photo thumbnail (Image if `item.photoUrl`), `item.name`, a `Published` Switch row and a `Featured` Switch row (same `Switch`/label markup as the table cells), and the same `DropdownMenu`/`DropdownMenuTrigger`/`DropdownMenuContent` Edit (Link to `/admin/packages/${item.id}`) + Delete (`AlertDialog` confirmation) block. In `components/admin/sortable-package-list.tsx`, wrap the existing `<Table>` element (including the `<DndContext>`/`<SortableContext>` currently inside it) in `<div className="hidden md:block">`. Add a second, `md:hidden`-wrapped `<DndContext sensors={sensors} collisionDetection={closestCenter} onDragEnd={handleDragEnd}><SortableContext items={items.map((item) => item.id)} strategy={verticalListSortingStrategy}><div className="flex flex-col gap-3">{items.map((item) => <PackageListCard key={item.id} item={item} onMutated={handleMutated} onDeleted={handleDeleted} />)}</div></SortableContext></DndContext>` sibling — reusing the same `sensors`/`handleDragEnd`/`items` state as the table branch (two `DndContext` instances may exist in the DOM since only the CSS-visible one receives pointer/keyboard events; this avoids restructuring the table's own `<DndContext>` placement). Import `PackageListCard` from "@/components/admin/package-list-card" in sortable-package-list.tsx. Do not modify package-list-row.tsx's own behavior — only read it as the source pattern (list it in files_modified only if you touch it; if no edit is needed there, leave it unmodified).

    D-07 hover/focus/active-state coverage: `package-list-row.tsx`'s drag-handle `<button>` (the source pattern being copied) currently carries zero hover/focus/active styling. Since `package-list-card.tsx` is a brand-new file (not an edit to package-list-row.tsx's own desktop markup, which stays untouched per the instruction above), add state classes to its drag-handle button's className that package-list-row.tsx's copy lacks: append `hover:bg-muted hover:text-foreground focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring/50 active:bg-muted rounded-md` to the existing `flex size-11 items-center justify-center text-muted-foreground` classes, using the same `ring-ring/50` focus-visible convention already established in `components/ui/checkbox.tsx`/`switch.tsx` — not a new token. The rest of the card's interactive surface needs no additional styling: the `Switch` rows already carry `focus-visible:ring-3 focus-visible:ring-ring/50` from `components/ui/switch.tsx`, and the `DropdownMenuTrigger` (a ghost `Button`) plus its `DropdownMenuItem` entries already carry `hover:`/`focus-visible:`/`focus:bg-accent` from their own shadcn base styles — reused verbatim with zero changes required to satisfy D-07 for those elements.
  </action>
  <acceptance_criteria>
    - `components/admin/package-list-card.tsx` exports `PackageListCard` and imports `useSortable` from "@dnd-kit/sortable".
    - `components/admin/package-list-card.tsx` imports `publishPackage`, `featurePackage`, `softDeletePackage` from "@/actions/packages" (same path as package-list-row.tsx) — no new action module.
    - `components/admin/sortable-package-list.tsx` contains both `hidden md:block` and `md:hidden` and imports `PackageListCard`.
    - `grep -c 'focus-visible:ring-2' components/admin/package-list-card.tsx` is >= 1 (drag-handle button's hover/focus/active-state per D-07).
    - `npm run build` completes with no new TypeScript errors.
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'export function PackageListCard' components/admin/package-list-card.tsx; grep -c 'from "@dnd-kit/sortable"' components/admin/package-list-card.tsx; grep -c 'from "@/actions/packages"' components/admin/package-list-card.tsx; grep -c 'md:hidden' components/admin/sortable-package-list.tsx; grep -c 'PackageListCard' components/admin/sortable-package-list.tsx; grep -c 'focus-visible:ring-2' components/admin/package-list-card.tsx; npm run build</automated>
  </verify>
  <done>Below 768px, /admin/packages renders packages as draggable Cards with working Published/Featured switches, edit/delete menu, and drag-reorder persisted via reorderPackages; at/above 768px, the existing Table renders exactly as before.</done>
</task>

<task type="auto">
  <name>Task 2: Add app/admin/(dashboard)/packages/loading.tsx skeleton</name>
  <files>app/admin/(dashboard)/packages/loading.tsx</files>
  <read_first>
    app/admin/(dashboard)/crm/loading.tsx (full file — the exact pattern to replicate)
    app/admin/(dashboard)/packages/page.tsx (to shape the skeleton toward drag-handle + thumbnail + name + 2 switches, per D-07; also confirms the existing `items.length === 0` empty-state block, lines ~94-105)
  </read_first>
  <action>
    Create `app/admin/(dashboard)/packages/loading.tsx` default-exporting `function PackagesLoading()`, mirroring `crm/loading.tsx`'s exact structure: a `flex flex-col gap-6` wrapper with a `Skeleton className="h-8 w-40"` title placeholder followed by a `flex flex-col gap-2` list of six `Skeleton className="h-14 w-full"` row placeholders (h-14, not crm's h-10, to loosely hint at the packages row's thumbnail+switches height — per RESEARCH.md's "do not over-engineer exact column widths" guidance, a uniform row skeleton is sufficient). Import `Skeleton` from "@/components/ui/skeleton". No data fetching, no client-side state — this is a static Server Component picked up automatically by Next.js's `loading.tsx` route-segment convention, no explicit route registration needed.

    D-07 empty-state coverage (packages): do NOT add a new empty-state block anywhere — `app/admin/(dashboard)/packages/page.tsx` already renders an `items.length === 0` "No packages yet" card (matching CRM's `bg-card p-8 ring-1 ring-foreground/10` pattern) in place of `<SortablePackageList>` entirely, before either the table or the new `md:hidden` card branch from Task 1 is ever reached. Because that check happens at the page level and short-circuits rendering `SortablePackageList` altogether, it already applies uniformly regardless of breakpoint — there is no separate "card-mode empty state" to build. Confirm this by inspecting `app/admin/(dashboard)/packages/page.tsx` (do not modify it) and record the confirmation explicitly in SUMMARY.md rather than omitting it, per D-07.
  </action>
  <acceptance_criteria>
    - File exists at `app/admin/(dashboard)/packages/loading.tsx` with a valid default-exported React component.
    - `grep -c 'Skeleton' "app/admin/(dashboard)/packages/loading.tsx"` is >= 2 (title + at least one row).
    - `grep -c 'No packages yet' "app/admin/(dashboard)/packages/page.tsx"` is >= 1, confirming the existing page-level empty state (which already covers both breakpoints) is present and unmodified.
    - `npm run build` completes with no new errors.
  </acceptance_criteria>
  <verify>
    <automated>test -f "app/admin/(dashboard)/packages/loading.tsx" &amp;&amp; echo 1 || echo 0; grep -c 'Skeleton' "app/admin/(dashboard)/packages/loading.tsx"; grep -c 'No packages yet' "app/admin/(dashboard)/packages/page.tsx"; npm run build</automated>
  </verify>
  <done>Navigating to /admin/packages shows a skeleton placeholder (not a blank flash) while the initial Supabase query resolves, and SUMMARY.md records the confirmation that packages' existing page-level empty state already covers card mode (D-07).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Admin browser -> publishPackage/featurePackage/softDeletePackage/reorderPackages Server Actions | Card-mode UI is a new rendering path into the same existing, permission-gated (`can_manage_packages`) Server Actions from Phase 2 — this plan does not touch those Server Actions themselves. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-------------------|
| T-05-04 | Elevation of Privilege | package-list-card.tsx | low | accept | Card mode calls the identical `publishPackage`/`featurePackage`/`softDeletePackage`/`reorderPackages` Server Actions already called by `package-list-row.tsx` — verified via Task 1's grep gate (`@/actions/packages` import path match) — so no new, unguarded call site is introduced and AUTH-05's server-side enforcement is untouched. |
| T-05-05 | — | packages/loading.tsx | low | accept | No new attack surface — a static skeleton with no data fetching or user input. |

</threat_model>

<verification>
- `npm run build` succeeds after both tasks.
- `grep -c 'md:hidden' components/admin/sortable-package-list.tsx` is >= 1.
- `test -f "app/admin/(dashboard)/packages/loading.tsx"` succeeds.
</verification>

<success_criteria>
- sortable-package-list.tsx renders draggable cards below 768px and the existing table at/above it, with zero new Server Action call sites.
- /admin/packages shows a skeleton instead of a blank flash on load.
</success_criteria>

<output>
Create `.planning/phases/05-mobile-responsive-visual-polish/05-03-SUMMARY.md` when done
</output>
