---
phase: 05-mobile-responsive-visual-polish
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - components/admin/users-table.tsx
  - app/admin/(dashboard)/users/loading.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Below the md (768px) breakpoint, /admin/users renders accounts as stacked cards instead of a raw table (D-03)"
    - "Navigating to /admin/users shows a skeleton placeholder instead of a blank flash while the initial Supabase query resolves (D-07)"
    - "An empty accounts list renders a 'No accounts yet' card instead of a bare table header, at any breakpoint (D-07)"
    - "Card-mode Edit/Deactivate controls keep their existing shadcn hover/focus/active defaults with zero new styling needed (D-07)"
  artifacts:
    - "app/admin/(dashboard)/users/loading.tsx — new UsersLoading route-level Suspense fallback"
  key_links:
    - "Card-mode Edit/Deactivate actions -> the SAME setEditingAccount/setDeactivatingAccount state and deactivateAccount Server Action already used by the table, no new call sites"
    - "The new profiles.length === 0 empty-state branch wraps BOTH the hidden md:block Table and md:hidden Card list, so it is viewport-agnostic like crm-table.tsx's existing hasNoMatches empty state"
---

<objective>
Retrofit `components/admin/users-table.tsx` so Admin/Staff accounts render as stacked cards below 768px (D-03), and add the missing `users/loading.tsx` skeleton (D-07's confirmed empty-state gap).

Purpose: `users-table.tsx` is a plain (no table library) `<Table>` — the simplest of the three admin tables to retrofit, with no row-selection or drag state to preserve. `/admin/users` currently has no `loading.tsx` (unlike `/admin/crm`), causing a blank-white flash on load.
Output: `users-table.tsx` with a CSS-only twin-markup table/card breakpoint switch, and `app/admin/(dashboard)/users/loading.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-mobile-responsive-visual-polish/05-CONTEXT.md
@.planning/phases/05-mobile-responsive-visual-polish/05-RESEARCH.md
@.planning/phases/05-mobile-responsive-visual-polish/05-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CSS-only card-mode branch to users-table.tsx</name>
  <files>components/admin/users-table.tsx</files>
  <read_first>
    components/admin/users-table.tsx (full file — Table render at lines 98-179, role/permission/status Badge logic at lines 117-149, DropdownMenu Edit/Deactivate block at lines 151-173, the Add/Edit Dialog and Deactivate AlertDialog at lines 85-95/181-223)
    components/ui/card.tsx
    components/admin/crm-table.tsx lines 249-260 (the existing "No contacts match your search" empty-state markup — the exact pattern to replicate for users, since users-table.tsx currently has no empty-state block at all)
    05-PATTERNS.md's users-table.tsx section
  </read_first>
  <action>
    Import `Card` from "@/components/ui/card" into components/admin/users-table.tsx. Wrap the existing `<Table>` render (the block starting `<div className="overflow-hidden rounded-xl border border-border">`) in `hidden md:block` — internals unchanged. Add a sibling `<div className="flex flex-col gap-3 md:hidden">` mapping over `profiles`, rendering one `<Card key={profile.id} className="p-4">` per profile containing: (a) `profile.name ?? "—"` and `profile.email` as primary/secondary text with the identical `DropdownMenu`/`DropdownMenuTrigger`/`DropdownMenuContent` Edit/Deactivate block reused verbatim (same `onClick={() => setEditingAccount(profile)}` / `onClick={() => setDeactivatingAccount(profile)}` handlers — no new state); (b) the identical role Badge (`profile.role === "admin" ? "default" : "secondary"` variant, "Admin"/"Staff" label); (c) the identical permission-badge block ("All" for admins, else conditional "Packages"/"Messages"/"CRM" outline Badges based on `can_manage_packages`/`can_message_customers`/`can_edit_crm`); (d) the identical status Badge (`profile.is_active ? "secondary" : "destructive"` variant, "Active"/"Inactive" label). Do not import or call `useIsMobile()` — pure CSS `hidden md:block`/`md:hidden` switch. The `Add Staff Account` Dialog trigger, the Edit `Dialog`, and the Deactivate `AlertDialog` (lines 85-95, 181-223) stay completely unchanged outside both branches — they are already viewport-agnostic overlays.

    D-07 empty-state coverage (users — genuine gap, confirmed by inspection): unlike `crm-table.tsx` and `packages/page.tsx`, `users-table.tsx` currently has no empty-state handling at all — if `profiles` is empty (a real path per this file's own error-handling comment in `users/page.tsx`: a Supabase query error still renders the page with `profiles ?? []`), the table renders a bare header row with no body rows. Add an `profiles.length === 0` branch, gated the same way as `crm-table.tsx`'s `hasNoMatches` block, that renders a `<div className="flex flex-col items-start gap-3 rounded-xl bg-card p-8 ring-1 ring-foreground/10">` containing a `font-heading text-[20px] leading-[1.2] font-semibold` heading reading "No accounts yet" and a `text-base leading-[1.5] text-muted-foreground` paragraph reading "Add a Staff or Admin account to get started." — matching CRM/packages' existing empty-state visual pattern exactly (same classes, same copy tone). No CTA button is needed inside this block since the "Add Staff Account" trigger already sits above it, unconditionally. Place this branch so it replaces BOTH the `hidden md:block` Table and the `md:hidden` Card list when `profiles.length === 0` (i.e. wrap the existing two responsive branches in `profiles.length === 0 ? (<div>...</div>) : (<>...</>)`), so the empty state is viewport-agnostic like CRM's.

    D-07 hover/focus/active-state coverage (users card, remainder): the card itself has no `onClick`/navigation (unlike crm-table.tsx's clickable card) — only its `DropdownMenuTrigger` (a ghost `Button`) and `DropdownMenuItem` Edit/Deactivate entries are interactive, and both already carry hover/focus/active treatment from their own shadcn base styles (`button.tsx`'s `hover:bg-muted`/`focus-visible:ring-3`/`active:not-aria-[haspopup]:translate-y-px`, `dropdown-menu.tsx`'s `focus:bg-accent`) reused verbatim with zero additional styling required to satisfy D-07 for this component.
  </action>
  <acceptance_criteria>
    - `grep -c 'md:hidden' components/admin/users-table.tsx` is >= 1 and `grep -c 'hidden md:block' components/admin/users-table.tsx` is >= 1.
    - `grep -c 'useIsMobile' components/admin/users-table.tsx` is exactly 0.
    - `grep -c 'setEditingAccount' components/admin/users-table.tsx` is >= 2 (existing table cell + new card branch, both calling the same setter).
    - `grep -c 'No accounts yet' components/admin/users-table.tsx` is >= 1 and `grep -c 'profiles.length === 0' components/admin/users-table.tsx` is >= 1 (new D-07 empty state, viewport-agnostic).
    - `npm run build` completes with no new TypeScript errors.
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'md:hidden' components/admin/users-table.tsx; grep -c 'hidden md:block' components/admin/users-table.tsx; grep -c 'useIsMobile' components/admin/users-table.tsx; grep -c 'setEditingAccount' components/admin/users-table.tsx; grep -c 'No accounts yet' components/admin/users-table.tsx; grep -c 'profiles.length === 0' components/admin/users-table.tsx; npm run build</automated>
  </verify>
  <done>Below 768px, /admin/users renders accounts as Cards with working Edit/Deactivate actions (carrying their existing shadcn hover/focus/active states, D-07) and identical role/permission/status badges; an empty `profiles` array shows a "No accounts yet" card instead of a bare header row, at any breakpoint; at/above 768px, the existing Table renders exactly as before.</done>
</task>

<task type="auto">
  <name>Task 2: Add app/admin/(dashboard)/users/loading.tsx skeleton</name>
  <files>app/admin/(dashboard)/users/loading.tsx</files>
  <read_first>
    app/admin/(dashboard)/crm/loading.tsx (full file — the exact pattern to replicate)
    app/admin/(dashboard)/users/page.tsx (to shape the skeleton toward name+email+role+permission+status+actions, per D-07)
  </read_first>
  <action>
    Create `app/admin/(dashboard)/users/loading.tsx` default-exporting `function UsersLoading()`, mirroring `crm/loading.tsx`'s exact structure: a `flex flex-col gap-6` wrapper with a `Skeleton className="h-8 w-40"` title placeholder followed by a `flex flex-col gap-2` list of six `Skeleton className="h-14 w-full"` row placeholders (h-14, hinting at the users row's badge-heavy layout). Import `Skeleton` from "@/components/ui/skeleton". Static Server Component, no data fetching.
  </action>
  <acceptance_criteria>
    - File exists at `app/admin/(dashboard)/users/loading.tsx` with a valid default-exported React component.
    - `grep -c 'Skeleton' "app/admin/(dashboard)/users/loading.tsx"` is >= 2.
    - `npm run build` completes with no new errors.
  </acceptance_criteria>
  <verify>
    <automated>test -f "app/admin/(dashboard)/users/loading.tsx" &amp;&amp; echo 1 || echo 0; grep -c 'Skeleton' "app/admin/(dashboard)/users/loading.tsx"; npm run build</automated>
  </verify>
  <done>Navigating to /admin/users shows a skeleton placeholder (not a blank flash) while the initial Supabase query resolves.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Admin browser -> deactivateAccount Server Action | Card-mode UI is a new rendering path into the same existing, admin-only (`requireAdminOrRedirect`) `deactivateAccount` Server Action from Phase 2 — this plan does not touch that Server Action, its self-/last-admin lockout guard, or its DB trigger backstop. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-------------------|
| T-05-06 | Elevation of Privilege | users-table.tsx card-mode actions | low | accept | Card mode calls the identical `setEditingAccount`/`setDeactivatingAccount` handlers and the identical `deactivateAccount` Server Action already used by the table — verified via Task 1's grep gate — so the existing self-/last-admin deactivation lockout guards (02-17) remain the sole enforcement path, untouched by this plan. |
| T-05-07 | — | users/loading.tsx | low | accept | No new attack surface — a static skeleton with no data fetching or user input. |

</threat_model>

<verification>
- `npm run build` succeeds after both tasks.
- `grep -c 'md:hidden' components/admin/users-table.tsx` is >= 1.
- `grep -c 'No accounts yet' components/admin/users-table.tsx` is >= 1.
- `test -f "app/admin/(dashboard)/users/loading.tsx"` succeeds.
</verification>

<success_criteria>
- users-table.tsx renders cards below 768px and the existing table at/above it, with zero new Server Action call sites.
- An empty accounts list shows a "No accounts yet" card rather than a bare table header, at any breakpoint (D-07).
- /admin/users shows a skeleton instead of a blank flash on load.
</success_criteria>

<output>
Create `.planning/phases/05-mobile-responsive-visual-polish/05-04-SUMMARY.md` when done
</output>
