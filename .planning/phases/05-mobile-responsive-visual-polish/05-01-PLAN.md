---
phase: 05-mobile-responsive-visual-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/packages/package-gallery.tsx
  - app/(public)/page.tsx
  - app/(public)/packages/page.tsx
  - app/(public)/packages/[slug]/page.tsx
  - app/(public)/contact/page.tsx
  - app/(public)/layout.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Every public page (/, /packages, /packages/[slug], /contact, inquiry form submitted/error state) renders with no horizontal overflow and no overlapping elements up to 200% browser zoom (D-02)"
    - "The gallery lightbox's carousel nav buttons stay inside the dialog's visible bounds at every breakpoint and zoom level"
  artifacts:
    - "components/packages/package-gallery.tsx — CarouselPrevious/CarouselNext no longer use a negative sm:-left-12/sm:-right-12 offset"
  key_links:
    - "CarouselPrevious/CarouselNext className -> Dialog's max-w-[calc(100%-2rem)] boundary — buttons must render inside it, not escape it, at 200%/400% zoom"
---

<objective>
Fix the reported "layout does not adjust when browser zoom level is adjusted" bug (D-01) and verify the fix against WCAG 1.4.10's 200%/400% zoom reflow bar (D-02) across all five in-scope public routes.

Purpose: RESEARCH.md identified a concrete, code-verifiable root cause — `components/packages/package-gallery.tsx`'s lightbox carousel nav buttons use `sm:-left-12`/`sm:-right-12` (48px negative offsets) that escape the Dialog's `max-w-[calc(100%-2rem)]` boundary at high zoom. A static grep sweep found no other public-site component with a similar overflow-prone pattern, but D-01's audit scope is "everywhere / not sure" — this plan fixes the known bug and then performs the live zoom pass the static analysis cannot substitute for.
Output: `package-gallery.tsx` fixed; a live-verified confirmation (or additional targeted fix) that all 5 public routes pass the 200%/400% zoom reflow bar.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-mobile-responsive-visual-polish/05-CONTEXT.md
@.planning/phases/05-mobile-responsive-visual-polish/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix gallery lightbox nav-button zoom-reflow overflow</name>
  <files>components/packages/package-gallery.tsx</files>
  <read_first>
    components/packages/package-gallery.tsx (full file — CarouselPrevious/CarouselNext usage at the bottom of the Carousel block)
    components/ui/carousel.tsx (confirms CarouselPrevious/CarouselNext accept a className prop merged via cn())
    components/ui/dialog.tsx (DialogContent's `max-w-[calc(100%-2rem)]` boundary that the buttons currently escape)
  </read_first>
  <action>
    In components/packages/package-gallery.tsx, on the `<CarouselPrevious>` element change `className="left-2 sm:-left-12"` to `className="left-2"`, and on `<CarouselNext>` change `className="right-2 sm:-right-12"` to `className="right-2"` — dropping the negative `sm:` offset entirely so both nav buttons stay inside DialogContent's own padding box at every breakpoint and zoom level, per D-01/D-02 and RESEARCH.md's Pitfall 1 / Pattern 3 root-cause analysis. Do not change any other prop of `Carousel`/`CarouselContent`/`CarouselItem`/`CarouselPrevious`/`CarouselNext` (loop/startIndex/opts, image sizing, and the thumbnail grid above stay untouched) — this is a targeted positioning fix, not a redesign.
  </action>
  <acceptance_criteria>
    - No occurrence of a negative offset utility (`-left-12`, `-right-12`, or any `sm:-left-`/`sm:-right-` variant) remains anywhere in the file.
    - `left-2` and `right-2` remain present on CarouselPrevious/CarouselNext respectively, unconditional across all breakpoints.
    - `npm run build` completes with no new TypeScript or lint errors.
  </acceptance_criteria>
  <verify>
    <automated>grep -c -- '-left-12\|-right-12' components/packages/package-gallery.tsx | grep -qx 0 && npm run build</automated>
  </verify>
  <done>CarouselPrevious/CarouselNext render at a fixed left-2/right-2 position inside the dialog's padding box at all breakpoints; no negative-offset utility remains in the file.</done>
</task>

<task type="auto">
  <name>Task 2: Live 200%/400% zoom-reflow audit across all 5 public routes</name>
  <files>app/(public)/page.tsx, app/(public)/packages/page.tsx, app/(public)/packages/[slug]/page.tsx, app/(public)/contact/page.tsx, app/(public)/layout.tsx</files>
  <read_first>
    app/(public)/layout.tsx (header/footer nav — already fluid flex-wrap, confirmed no negative-offset/fixed-width patterns via static grep sweep)
    app/(public)/page.tsx, app/(public)/packages/page.tsx, app/(public)/packages/[slug]/page.tsx, app/(public)/contact/page.tsx
    05-RESEARCH.md's "WCAG 1.4.10 Reflow test protocol" code block and Assumptions Log entries A1/A2
  </read_first>
  <action>
    Run `npm run dev`. For each of the 5 in-scope surfaces — `/`, `/packages`, `/packages/[slug]` (any seeded published package), `/contact`, and the inquiry form's submitted/error state on `/contact` — set actual browser zoom (Cmd/Ctrl + "+", NOT DevTools' device-width responsive simulator, which does not reproduce this bug class per RESEARCH.md's Anti-Patterns) to 200% and confirm: no horizontal scrollbar on the page body, no two elements visually overlapping, and every interactive element (nav links, package cards, WhatsApp/Facebook CTAs, form fields, the now-fixed gallery lightbox nav buttons) remains fully visible and clickable. Repeat at 400% for at least the text-heavy `/packages/[slug]` route per RESEARCH.md's protocol. RESEARCH.md's static grep sweep (Assumption A2) found no other component with a negative-offset/fixed-width/100vw-without-guard pattern beyond the gallery fixed in Task 1 — if the live pass surfaces an additional concrete overflow the static sweep missed, fix it directly in the offending file using the same fluid `max-w-*`/`flex-wrap`/`grid-cols-*` approach already used throughout the public site (Rule 1, same D-01/D-02 acceptance bar), and note the file + fix in this plan's SUMMARY. If the live pass finds no additional issues, record that explicit pass/fail confirmation per route/zoom-level in the SUMMARY instead of guessing.
  </action>
  <acceptance_criteria>
    - SUMMARY.md documents an explicit pass/fail result for each of the 5 routes at 200% zoom, and for `/packages/[slug]` at 400% zoom.
    - If an additional fix was made, it introduces no new negative-offset/fixed-px-width/box-shadow anti-pattern (grep-verifiable) and `npm run build` still passes.
    - If no additional fix was needed, the SUMMARY states that explicitly rather than omitting the audit result.
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
    <human-check>Perform the 200%/400% browser-zoom pass described in the action across all 5 routes and confirm: no horizontal scrollbar, no overlapping elements, no clipped interactive elements. Record the result in SUMMARY.md (workflow.human_verify_mode=end-of-phase — this is the authoritative check for D-02's acceptance bar, since no browser-automation tooling is installed in this project).</human-check>
  </verify>
  <done>All 5 public routes are confirmed (live, not just statically) to meet D-02's zoom-reflow bar, or any additional finding is fixed and documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | This plan only repositions existing static UI elements via CSS class changes on already-published-package data; no new input handling, no new data path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-------------------|
| T-05-01 | — | package-gallery.tsx / public pages | low | accept | No new attack surface — pure CSS positioning fix and a live-verification pass over already-published, already-server-filtered (`is_published=true`) package data. No auth, session, or data-mutation logic is touched. |

</threat_model>

<verification>
- `npm run build` succeeds with zero new errors after both tasks.
- `grep -c -- '-left-12\|-right-12' components/packages/package-gallery.tsx` returns 0.
- SUMMARY.md documents the live 200%/400% zoom pass result per route.
</verification>

<success_criteria>
- Gallery lightbox nav buttons never escape the dialog's visible bounds at any zoom level.
- All 5 public routes are live-verified to meet the WCAG 1.4.10-style 200% (and 400% for the detail page) reflow bar from D-02, or any additional root cause found during the live pass is fixed and documented.
</success_criteria>

<output>
Create `.planning/phases/05-mobile-responsive-visual-polish/05-01-SUMMARY.md` when done
</output>
