---
phase: 06-public-site-content-sections-hero-carousel
plan: 07
type: execute
wave: 3
depends_on: ["06-03", "06-04"]
files_modified:
  - app/(public)/page.tsx
autonomous: true
requirements: [HOME-01, HOME-02, HOME-03, HOME-04, HOME-05, HOME-06, HOME-07]

must_haves:
  truths:
    - "A package-type hero slide whose linked package has since been unpublished or soft-deleted is silently skipped from the rendered carousel, never rendered broken (RESEARCH.md Pitfall 1)"
    - "Brand Partners and Corporate Clients visibility is determined by two fully independent queries/counts, never a single combined check (RESEARCH.md Pitfall 3 / D-07)"
    - "The embedded inquiry form section renders the existing <InquiryForm /> with zero new props -- packageName and packageId both stay undefined, exactly matching a general (non-package) inquiry"
    - "Every section renders in the UI-SPEC-specified order: Hero Carousel, Why Choose Us, Featured Packages Grid, Testimonials, Inquiry Form, Brand Partners, Corporate Clients"
  artifacts:
    - path: "app/(public)/page.tsx"
      provides: "async Server Component composing all 6 new homepage sections plus the existing InquiryForm, fetching every dataset server-side"
  key_links:
    - from: "app/(public)/page.tsx"
      to: "components/homepage/{hero-carousel,why-choose-us,featured-packages-grid,testimonials-section,brand-partners,corporate-clients}.tsx (06-03/06-04)"
      via: "server-fetched, already-URL-resolved props passed down to each presentational component"
      pattern: "from \"@/components/homepage/"
    - from: "app/(public)/page.tsx"
      to: "components/inquiry/inquiry-form.tsx (existing, Phase 1/3, unchanged)"
      via: "<InquiryForm /> with no props -- identical to app/(public)/contact/page.tsx's existing usage"
      pattern: "<InquiryForm />"
    - from: "app/(public)/page.tsx"
      to: "hero_slides -> packages LEFT JOIN (orphan-skip filter)"
      via: "supabase.from(\"hero_slides\").select(\"*, packages(...)\") then filter(slide => slide.slide_type === \"promo\" || slide.packages !== null)"
      pattern: "slide\\.packages !== null"
---

<objective>
## Phase Goal

The homepage gains a hero carousel and admin-editable content sections -- value props, a featured packages grid, customer testimonials, an embedded inquiry form, and conditional Brand Partners/Corporate Clients logo sections -- all within the existing locked brand system.

(Same phase-level goal as 06-01-PLAN.md.)

## This Plan

Rewrite `app/(public)/page.tsx` from its current static 2-paragraph hero into the full homepage composition: an `async` Server Component that fetches all 6 new datasets (hero slides with orphan-skip filtering, value props, featured packages, testimonials, brand partners, corporate clients), resolves every Storage/package-photo public URL server-side, and renders the 7 sections in UI-SPEC's specified order -- including the existing `<InquiryForm />` reused completely unchanged (D-05, HOME-07 -- zero new code beyond the import). This is the phase's final integration point: it depends on 06-03 and 06-04 (all 6 presentational components must exist), but NOT on 06-02/06-05/06-06 (the admin CRUD surfaces) -- the homepage only ever reads, never writes. Runs in parallel with 06-05/06-06 (wave 3, no file overlap).

Purpose: Deliver the actual, visitor-facing homepage this phase exists to build -- the point where every other plan's work becomes visible.
Output: A rewritten `app/(public)/page.tsx`, verified via `npm run build` and this plan's own structural greps for the two hardest-to-get-right query patterns (orphan-skip, independent dual-count).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-public-site-content-sections-hero-carousel/06-CONTEXT.md
@.planning/phases/06-public-site-content-sections-hero-carousel/06-RESEARCH.md
@.planning/phases/06-public-site-content-sections-hero-carousel/06-PATTERNS.md
@.planning/phases/06-public-site-content-sections-hero-carousel/06-UI-SPEC.md
@app/(public)/page.tsx
@app/(public)/packages/page.tsx
@app/(public)/contact/page.tsx
@components/inquiry/inquiry-form.tsx
@components/homepage/hero-carousel.tsx
@components/homepage/why-choose-us.tsx
@components/homepage/featured-packages-grid.tsx
@components/homepage/testimonials-section.tsx
@components/homepage/brand-partners.tsx
@components/homepage/corporate-clients.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Data-fetching layer -- all 6 queries + orphan-skip + URL resolution</name>
  <files>app/(public)/page.tsx</files>
  <read_first>
    - app/(public)/page.tsx (current 23-line file -- the static baseline this task replaces with an async Server Component)
    - app/(public)/packages/page.tsx (full file -- existing query + photoUrl-resolution shape this task's featured-packages query mirrors exactly)
    - .planning/phases/06-public-site-content-sections-hero-carousel/06-RESEARCH.md lines 254-286 (Architecture Patterns 2 and 3 -- the exact orphan-skip filter and independent dual-count query shapes, verbatim starting point) and Pitfall 1 (why the filter is necessary -- hero_slides is unconditionally public-read but packages is not)
    - components/homepage/hero-carousel.tsx (06-03's `HeroSlideDisplay` type -- this task's query output must be shaped to exactly match it)
  </read_first>
  <action>
    Rewrite `app/(public)/page.tsx` as an `async function HomePage()` Server Component. Keep the existing `Metadata` export pattern used by `app/(public)/packages/page.tsx`/`app/(public)/contact/page.tsx` if adding one improves SEO, but at minimum preserve the page rendering successfully with no metadata regression. `const supabase = await createClient();` (from `@/lib/supabase/server`). Fetch, in this order:

    (1) Hero slides: `supabase.from("hero_slides").select("*, packages(id, slug, name, is_published, deleted_at, package_photos(storage_path, display_order))").order("sort_order", { ascending: true })`. Filter: `const slides = (rawSlides ?? []).filter((slide) => slide.slide_type === "promo" || slide.packages !== null);` (Pitfall 1's orphan-skip -- a package-type slide whose joined `packages` came back null under RLS, because the linked package is unpublished/deleted, must never render). Map each surviving slide to `HeroSlideDisplay`: for `slide_type === "package"`, `headline = slide.packages.name`, `ctaLabel = "View Package"`, `ctaHref = \`/packages/${slide.packages.slug}\``, `imageUrl` resolved from the package's first `package_photos` row (sorted by `display_order`) via `supabase.storage.from("package-photos").getPublicUrl(...)`; for `slide_type === "promo"`, `headline = slide.headline`, `ctaLabel = slide.cta_label || null`, `ctaHref = slide.cta_label ? slide.external_link ?? null : null` (no CTA rendered at all when `cta_label` is empty, per UI-SPEC's Copywriting Contract), `imageUrl` resolved from `slide.image_storage_path` via `supabase.storage.from("site-content").getPublicUrl(...)`.

    (2) Value props: `supabase.from("value_props").select("*").order("sort_order", { ascending: true })`.

    (3) Featured packages: `supabase.from("packages").select("*, package_photos(storage_path, display_order)").eq("is_published", true).eq("is_featured", true).order("sort_order", { ascending: true }).limit(6)` (byte-identical query shape to RESEARCH.md's Code Example 2 -- zero new curation code, D-04). Map each row to `{ pkg, photoUrl }` using the identical first-photo-by-display_order + `package-photos` bucket `getPublicUrl` resolution already used in `app/(public)/packages/page.tsx`.

    (4) Testimonials: `supabase.from("testimonials").select("*").order("sort_order", { ascending: true })`. Resolve `photoUrl` from `photo_storage_path` via `supabase.storage.from("site-content").getPublicUrl(...)` when set, else `null`.

    (5) Brand partners: `supabase.from("partners").select("*").eq("partner_type", "brand_partner").order("sort_order", { ascending: true })` -- a fully separate query and separate array from (6) below (RESEARCH.md Pitfall 3 / D-07 -- never a combined "any partner" check).

    (6) Corporate clients: `supabase.from("partners").select("*").eq("partner_type", "corporate_client").order("sort_order", { ascending: true })`. Resolve both (5) and (6)'s `logoUrl` from `logo_storage_path` via the `site-content` bucket.

    Log any query error server-side via `console.error` (matching `app/(public)/packages/page.tsx`'s existing pattern) and fall back to an empty array for that section rather than crashing the whole page on a transient DB error -- every downstream component already handles an empty array by rendering null.
  </action>
  <acceptance_criteria>
    - `grep -c 'slide.slide_type === "promo" || slide.packages !== null' app/(public)/page.tsx` == 1 (orphan-skip filter present, exact Pitfall 1 shape)
    - `grep -c 'from("partners").select' app/(public)/page.tsx` == 2 (two fully independent queries, one per partner_type)
    - `grep -c 'eq("partner_type", "brand_partner")' app/(public)/page.tsx` == 1
    - `grep -c 'eq("partner_type", "corporate_client")' app/(public)/page.tsx` == 1
    - `grep -c 'eq("is_featured", true)' app/(public)/page.tsx` == 1
    - `grep -c 'from("site-content")' app/(public)/page.tsx` >= 1
    - `grep -c 'async function HomePage' app/(public)/page.tsx` == 1
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'slide.slide_type === "promo" || slide.packages !== null' "app/(public)/page.tsx"; grep -c 'from("partners").select' "app/(public)/page.tsx"; grep -c 'eq("partner_type", "brand_partner")' "app/(public)/page.tsx"; grep -c 'eq("partner_type", "corporate_client")' "app/(public)/page.tsx"; grep -c 'eq("is_featured", true)' "app/(public)/page.tsx"; grep -c 'from("site-content")' "app/(public)/page.tsx"; grep -c 'async function HomePage' "app/(public)/page.tsx"</automated>
  </verify>
  <done>app/(public)/page.tsx fetches all 6 datasets server-side with the orphan-skip filter and two fully independent partner_type queries, resolving every image URL before render.</done>
</task>

<task type="auto">
  <name>Task 2: JSX composition -- section order, InquiryForm reuse, empty-state pass-through</name>
  <files>app/(public)/page.tsx</files>
  <read_first>
    - app/(public)/page.tsx (Task 1's output -- the data-fetching layer this task renders against)
    - app/(public)/contact/page.tsx (full file -- the exact `<InquiryForm />` no-props usage this task copies verbatim for HOME-07)
    - .planning/phases/06-public-site-content-sections-hero-carousel/06-UI-SPEC.md lines 99-109 (Section Layout & Composition -- the exact 7-section top-to-bottom order and per-section background/spacing) and lines 122 (Copywriting Contract -- Contact section heading "Get in Touch")
  </read_first>
  <action>
    Complete `app/(public)/page.tsx`'s render, composing sections in this exact order: `<HeroCarousel slides={slides} />`, `<WhyChooseUs valueProps={valueProps} />`, `<FeaturedPackagesGrid items={featuredItems} />`, `<TestimonialsSection testimonials={testimonials} />`, a `<section>` with a Display-size heading reading `"Get in Touch"` wrapping `<InquiryForm />` (imported from `@/components/inquiry/inquiry-form`, called with ZERO props -- `packageName`/`packageId` both stay `undefined`, matching `app/(public)/contact/page.tsx`'s exact existing usage, D-05/HOME-07), `<BrandPartners partners={brandPartners} />`, `<CorporateClients clients={corporateClients} />`. Do not reorder, do not add a "coming soon" fallback for any section -- every one of the 6 new components already handles its own empty-array case by rendering `null` (UI-SPEC's public homepage empty-state rule), so this page never needs its own conditional wrapping around them. Import all 6 homepage components from `@/components/homepage/*` and `InquiryForm` from `@/components/inquiry/inquiry-form`.
  </action>
  <acceptance_criteria>
    - `grep -c '<HeroCarousel' app/(public)/page.tsx` == 1
    - `grep -c '<WhyChooseUs' app/(public)/page.tsx` == 1
    - `grep -c '<FeaturedPackagesGrid' app/(public)/page.tsx` == 1
    - `grep -c '<TestimonialsSection' app/(public)/page.tsx` == 1
    - `grep -c '<InquiryForm />' app/(public)/page.tsx` == 1 (zero props, matching contact/page.tsx's exact usage)
    - `grep -c '<BrandPartners' app/(public)/page.tsx` == 1
    - `grep -c '<CorporateClients' app/(public)/page.tsx` == 1
    - `grep -c 'Get in Touch' app/(public)/page.tsx` == 1
    - Section order verified by line-number ordering: `grep -n '<HeroCarousel\|<WhyChooseUs\|<FeaturedPackagesGrid\|<TestimonialsSection\|<InquiryForm />\|<BrandPartners\|<CorporateClients' app/(public)/page.tsx` returns exactly this sequence
    - `npm run build` completes with no new TypeScript errors
  </acceptance_criteria>
  <verify>
    <automated>grep -c '<HeroCarousel' "app/(public)/page.tsx"; grep -c '<WhyChooseUs' "app/(public)/page.tsx"; grep -c '<FeaturedPackagesGrid' "app/(public)/page.tsx"; grep -c '<TestimonialsSection' "app/(public)/page.tsx"; grep -c '<InquiryForm />' "app/(public)/page.tsx"; grep -c '<BrandPartners' "app/(public)/page.tsx"; grep -c '<CorporateClients' "app/(public)/page.tsx"; grep -c 'Get in Touch' "app/(public)/page.tsx"; grep -n '<HeroCarousel\|<WhyChooseUs\|<FeaturedPackagesGrid\|<TestimonialsSection\|<InquiryForm />\|<BrandPartners\|<CorporateClients' "app/(public)/page.tsx"; npm run build</automated>
  </verify>
  <done>app/(public)/page.tsx composes all 7 sections in UI-SPEC's exact order, reuses <InquiryForm /> with zero new props, and relies entirely on each component's own empty-state handling; npm run build succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Unauthenticated visitor -> app/(public)/page.tsx's 6 Supabase queries | The single highest-traffic public route in the app -- every query here runs against anon-key RLS, no session required |
| hero_slides -> packages join | Cross-table trust asymmetry: hero_slides is unconditionally public-read, packages is not (Pitfall 1) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-18 | Information Disclosure | A package-type hero slide referencing an unpublished/deleted package leaking that package's existence, name, or photo to an anon visitor | medium | mitigate | The orphan-skip filter (`slide.slide_type === "promo" \|\| slide.packages !== null`, Task 1) drops any package-type slide whose joined `packages` came back null under RLS -- verified by this plan's acceptance criteria grep. This closes the exact leak scenario 06-01's threat T-06-03 flagged as deferred to this plan. |
| T-06-19 | Tampering | Admin-entered headline/subheading/quote/title/description text rendered via JSX across 6 components | low | mitigate | React's JSX auto-escapes all string interpolation across every component this page composes -- no `dangerouslySetInnerHTML` anywhere in the homepage component tree (06-03/06-04 already verified this per-component) |
| T-06-20 | Denial of Service | 6 sequential Supabase queries on the homepage's critical render path, no caching | low | accept | Matches this project's existing pattern (`app/(public)/packages/page.tsx` also runs uncached per-request queries) -- Next.js's default fetch/route caching plus Supabase's connection pooling is the accepted baseline for this project's traffic scale; a future phase can add `unstable_cache`/ISR if homepage load becomes a measured problem, not a speculative one |

</threat_model>

<verification>
`npm run build` compiles cleanly. Structural grep checks confirm: the orphan-skip filter is present verbatim, Brand Partners/Corporate Clients are two fully independent queries, the featured-packages query reuses the existing is_featured flag with zero new curation code, InquiryForm is invoked with zero props, and all 7 sections render in UI-SPEC's exact order.
</verification>

<success_criteria>
- Homepage renders all 6 new sections plus the embedded inquiry form, each populated from a live Supabase query
- A hero slide referencing an unpublished/deleted package never renders broken (orphan-skip verified)
- Brand Partners and Corporate Clients are independently visible/hidden based on their own counts
- The inquiry form on the homepage is byte-identical in behavior to /contact's existing form (same component, same pipeline, no packageId/packageName)
- npm run build succeeds
</success_criteria>

<output>
Create `.planning/phases/06-public-site-content-sections-hero-carousel/06-07-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- `app/(public)/page.tsx` (REWRITTEN) -- async Server Component fetching hero_slides (orphan-skip filtered), value_props, featured packages (is_featured=true), testimonials, and 2 independent partners queries (brand_partner/corporate_client), composing HeroCarousel -> WhyChooseUs -> FeaturedPackagesGrid -> TestimonialsSection -> InquiryForm -> BrandPartners -> CorporateClients
