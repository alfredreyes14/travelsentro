---
phase: 01-public-catalog-inquiry-entry-point
plan: 07
type: execute
wave: 6
depends_on: ["01-01", "01-04", "01-05", "01-06"]
files_modified:
  - app/(public)/layout.tsx
  - app/(public)/packages/page.tsx
  - app/(public)/packages/[slug]/page.tsx
  - app/(public)/contact/page.tsx
  - components/packages/package-card.tsx
autonomous: false
requirements: [PUBL-09]

must_haves:
  truths:
    - "Package list, package detail, and Contact Us pages are all fully usable at 375px, 768px, and 1024px+ viewport widths"
    - "All 5 ROADMAP Phase 1 success criteria hold on a real, deployed-or-local running instance"
    - "Icon-only CTA buttons meet the 44px minimum touch target on every viewport"
  artifacts:
    - path: "app/(public)/layout.tsx"
      provides: "Responsive nav confirmed/adjusted for narrow viewports"
  key_links:
    - from: "app/(public)/packages/page.tsx"
      to: "Tailwind responsive grid classes"
      via: "grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 (or equivalent) confirmed present"
      pattern: "grid-cols-1"
---

<objective>
Audit and refine every public page built across this phase's earlier plans for mobile responsiveness (PUBL-09), then run the final full-phase human verification confirming all 5 ROADMAP Phase 1 success criteria against the real, running application.

Purpose: Every earlier plan built its Tailwind classes with mobile-first responsiveness in mind, but no plan has yet been checked at real breakpoints end-to-end across the whole site. This plan is a refinement + verification pass, not the first time responsiveness is addressed — it closes gaps rather than retrofitting responsiveness that was never attempted.
Output: A verified, phase-complete public site satisfying every PUBL requirement on any device.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-public-catalog-inquiry-entry-point/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Responsive audit and fixes across all public pages</name>
  <files>app/(public)/layout.tsx, app/(public)/packages/page.tsx, app/(public)/packages/[slug]/page.tsx, app/(public)/contact/page.tsx, components/packages/package-card.tsx</files>
  <read_first>
    - app/(public)/layout.tsx, app/(public)/packages/page.tsx, app/(public)/packages/[slug]/page.tsx, app/(public)/contact/page.tsx, components/packages/package-card.tsx (all five files as built by 01-01, 01-04, 01-05, 01-06 — this task edits, not recreates, them)
    - .planning/phases/01-public-catalog-inquiry-entry-point/01-UI-SPEC.md lines 30-44 (Spacing scale + 44px minimum touch-target exception for icon-only CTAs)
  </read_first>
  <action>
    Review `app/(public)/layout.tsx`'s nav: confirm the 3 nav links (Home/Packages/Contact Us) remain legible and tappable at 375px width without horizontal overflow or text truncation; adjust Tailwind flex/gap/wrap classes if any link crowds or clips.
    Review `app/(public)/packages/page.tsx`'s grid: confirm it uses an explicit mobile-first responsive class chain (single column by default, expanding at `sm`/`md`/`lg` breakpoints) — tighten spacing/padding values against the UI-SPEC spacing scale (`md`=16px card padding, `xl`=32px between major sections) if any breakpoint looks cramped or excessively sparse.
    Review `app/(public)/packages/[slug]/page.tsx`: confirm the gallery thumbnail grid, itinerary accordion, checklist sections, trip-facts accordion, CTA row, and inquiry form all stack cleanly in a single column below `sm` and use the `2xl`/`3xl` spacing tokens between major sections (itinerary/inclusions/gallery/FAQ) as originally specified in UI-SPEC.
    Review `app/(public)/contact/page.tsx`: confirm the form is full-width with comfortable touch-sized inputs on mobile and a constrained max-width on desktop.
    Review `components/packages/package-card.tsx`: verify both icon-only CTA buttons measure at least 44x44px (padding, not just icon size) at every breakpoint — this is a hard UI-SPEC exception to the 8-point spacing scale, not optional.
    Fix any gaps found; do not introduce any new shadcn component beyond UI-SPEC's Registry Safety list while making these adjustments — this is a CSS/class-level refinement pass, not a component-architecture change.
  </action>
  <acceptance_criteria>
    - `app/(public)/packages/page.tsx` grid className contains at least one `sm:`/`md:`/`lg:` responsive grid-column utility in addition to the mobile-default single column
    - `components/packages/package-card.tsx` CTA button classNames include an explicit min-width/min-height (or padding equivalent) satisfying the 44px minimum
    - No new file is created and no new shadcn/ui component import is introduced in any of the 5 edited files
  </acceptance_criteria>
  <verify>
    <automated>npm run build 2>&1 | tail -30</automated>
  </verify>
  <done>All 5 public-facing files reviewed and adjusted for mobile responsiveness; build still passes cleanly with no new components introduced.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Full Phase 1 acceptance verification</name>
  <files>none (verification-only task, no files modified)</files>
  <action>
    Run `npm run dev` (or open the deployed preview if one exists) and manually walk through all 5 checks listed under `how-to-verify` below, at each of the 3 listed viewport widths, against the fully-built Phase 1 site. This task performs no code changes — it is the final blocking human checkpoint confirming every ROADMAP Phase 1 success criterion and every PUBL requirement is satisfied before the phase is considered complete.
  </action>
  <what-built>
    The complete Phase 1 public catalog and inquiry entry point: package list, package detail (itinerary/inclusions/gallery/trip-facts), WhatsApp/Facebook CTAs, native Formspree-backed inquiry form (per-package and Contact Us), all responsive across breakpoints.
  </what-built>
  <how-to-verify>
    Run `npm run dev` (or use a deployed preview URL if one exists) and check all 5 ROADMAP Phase 1 success criteria directly, each at 375px, 768px, and 1024px+ viewport widths:
    1. Browse `/packages` — each of the 3 seeded packages shows photo, name, and "From ₱X" price at every breakpoint.
    2. Open a package detail page — confirm day-by-day itinerary, line-item price/inclusions/exclusions, photo gallery (thumbnails + working lightbox), and FAQ/trip-facts (best time to go, what to bring, group size) all render and are usable at every breakpoint.
    3. From both the package card and the package detail page, confirm the WhatsApp deep link and Facebook page link both open correctly with no checkout step anywhere on the site.
    4. Submit the Contact Us form AND a per-package inquiry form with valid data — confirm both post successfully to Formspree and show a success toast; submit one with an invalid email — confirm the inline validation error matches UI-SPEC copy with no network call.
    5. Confirm the entire flow (list → detail → inquiry) is comfortably usable on a 375px-wide viewport with no horizontal scroll, no overlapping elements, and all CTA/tap targets easily tappable.
  </how-to-verify>
  <resume-signal>Type "approved" once all 5 criteria pass at all 3 breakpoints, or list which criteria/breakpoints failed.</resume-signal>
  <verify>
    <human-check>All 5 ROADMAP Phase 1 success criteria pass at 375px, 768px, and 1024px+ viewport widths</human-check>
  </verify>
  <done>Human verifier types "approved" confirming all 5 criteria pass at all 3 breakpoints.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | This plan makes presentation-only (CSS/layout) adjustments to already-reviewed files; it introduces no new server logic, data query, or external integration |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-18 | (none — presentation-only change) | Responsive CSS classes across public pages | low | accept | No new trust boundary, data flow, or external call is introduced by this plan; all changes are Tailwind className adjustments to already-secured (`01-05`, `01-06`) query and rendering paths |

</threat_model>

<verification>
`npm run build` succeeds. The checkpoint's full-phase manual verification confirms all 5 ROADMAP Phase 1 success criteria at 3 breakpoints.
</verification>

<success_criteria>
- Package list, detail, and inquiry flows are fully usable on mobile, tablet, and desktop viewports
- All 5 ROADMAP Phase 1 success criteria verified against the real running application
- All 9 PUBL requirements (PUBL-01 through PUBL-09) satisfied
</success_criteria>

<output>
Create `.planning/phases/01-public-catalog-inquiry-entry-point/01-07-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- No new files — this plan refines existing files (`app/(public)/layout.tsx`, `app/(public)/packages/page.tsx`, `app/(public)/packages/[slug]/page.tsx`, `app/(public)/contact/page.tsx`, `components/packages/package-card.tsx`) for responsive polish only
