---
phase: 01-public-catalog-inquiry-entry-point
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - scripts/seed.ts
  - supabase/seed-assets/*.jpg
  - package.json
autonomous: true
requirements: [PUBL-01, PUBL-02, PUBL-03, PUBL-04, PUBL-08]

must_haves:
  truths:
    - "At least 3 placeholder packages exist in the live packages table after seeding"
    - "Each seeded package has at least one real Storage-hosted photo, 3+ itinerary days, inclusions, exclusions, bring items, and faq_facts"
    - "Re-running the seed script does not create duplicate package rows"
  artifacts:
    - path: "scripts/seed.ts"
      provides: "Idempotent (upsert-on-slug) seed script for placeholder PH travel packages, run via `npm run seed`"
    - path: "supabase/seed-assets/*.jpg"
      provides: "Local placeholder photo files uploaded into the package-photos Storage bucket"
  key_links:
    - from: "scripts/seed.ts"
      to: "Supabase Storage package-photos bucket"
      via: "supabase.storage.from('package-photos').upload(path, image, ...)"
      pattern: "storage\\.from\\('package-photos'\\)\\.upload"
    - from: "scripts/seed.ts"
      to: "packages table"
      via: "supabase.from('packages').upsert({...}, { onConflict: 'slug' })"
      pattern: "\\.upsert\\("
---

<objective>
Write and run the idempotent seed script that populates the live Supabase schema (from `01-02`) with realistic placeholder Philippines tour packages — including real Storage-hosted photos, day-by-day itineraries, inclusions/exclusions/bring-items, and trip facts — so every subsequent UI plan has real data to query and render, satisfying D-01 and D-02.

Purpose: D-02 requires placeholder data now (real content arrives later from the business); D-01 requires that data be repo-committed and re-runnable, never manually entered via the Supabase dashboard. This plan is the one and only place Phase 1 writes to the database.
Output: 3+ fully-populated placeholder packages live in Supabase, ready for the package list and detail page plans to query.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-public-catalog-inquiry-entry-point/01-RESEARCH.md
@.planning/phases/01-public-catalog-inquiry-entry-point/01-CONTEXT.md

<interfaces>
From supabase/migrations/*_create_package_schema.sql (created in 01-02) — the exact table/column shape this script writes to:
packages(id, slug, name, from_price, duration_days, duration_label, is_published, is_featured, sort_order, created_at)
package_photos(id, package_id, storage_path, display_order, alt_text)
itinerary_days(id, package_id, day_number, title, description)
package_inclusions(id, package_id, kind ['included'|'excluded'|'bring'], label, sort_order)
faq_facts(id, package_id, best_time_to_go, group_size)
Storage bucket: package-photos (public)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write scripts/seed.ts and source placeholder photo assets</name>
  <files>scripts/seed.ts, supabase/seed-assets/*.jpg, package.json</files>
  <read_first>
    - .planning/phases/01-public-catalog-inquiry-entry-point/01-RESEARCH.md lines 458-486 (Code Examples — seed script sketch, use as the structural base)
    - .planning/phases/01-public-catalog-inquiry-entry-point/01-RESEARCH.md lines 513-517 (Open Question #1 — resolved: upsert on packages.slug for package rows, delete+reinsert child rows for the affected package_id on each run)
    - .planning/phases/01-public-catalog-inquiry-entry-point/01-PATTERNS.md lines 211-245 (seed.ts canonical pattern + re-runnability + security constraint: service-role key never imported from app/ or components/)
    - .planning/phases/01-public-catalog-inquiry-entry-point/01-CONTEXT.md lines 16-19 (D-01, D-02 — repo-committed, re-runnable, placeholder content)
  </read_first>
  <action>
    Source 3 sets of placeholder JPG images (at least 2 photos per package, 6+ files total) into `supabase/seed-assets/` — small (under ~300KB each) royalty-free/placeholder travel photos, named descriptively (e.g. `palawan-1.jpg`, `palawan-2.jpg`, `siargao-1.jpg`, `siargao-2.jpg`, `banaue-1.jpg`, `banaue-2.jpg`). These are committed to the repo, not fetched at runtime — the seed script reads them from disk and uploads them into the real Storage bucket, exercising the same upload path Phase 2's admin photo-upload feature will use (do not substitute a hotlinked external image URL for this step — that would skip the Storage upload path entirely and defeat D-01/D-02's purpose).
    Write `scripts/seed.ts` (Node script run via `tsx`, using `@supabase/supabase-js` directly with `SUPABASE_URL` and `SUPABASE_SERVICE_ROLE_KEY` from `process.env` — never import this file from `app/` or `components/`): define an array of 3 placeholder package objects covering distinct PH destinations (e.g. Palawan Island Hopping, Siargao Surf & Island, Banaue Rice Terraces), each with slug, name, from_price (integer PHP), duration_days, duration_label, is_published: true, is_featured (true for exactly one package, to exercise the Featured badge), sort_order (0, 1, 2).
    For each package: `supabase.from('packages').upsert({...}, { onConflict: 'slug' }).select().single()` to get the package id idempotently. Then, for that package_id, first delete any existing `package_photos`/`itinerary_days`/`package_inclusions`/`faq_facts` rows (`.delete().eq('package_id', id)`), then insert fresh: 2 photo rows (uploading the corresponding local JPG via `supabase.storage.from('package-photos').upload(\`${id}/photo-N.jpg\`, fileBuffer, { contentType: 'image/jpeg', upsert: true })` before inserting the row with that `storage_path`), 3+ `itinerary_days` rows (day_number 1..N, title, description), 3+ `package_inclusions` rows with `kind: 'included'`, 2+ with `kind: 'excluded'`, 3+ with `kind: 'bring'`, and exactly one `faq_facts` row (best_time_to_go, group_size).
    Add `"seed": "tsx --env-file=.env.local scripts/seed.ts"` to `package.json`'s `scripts` block (Node's built-in `--env-file` flag loads `.env.local` for this standalone script, since Next.js's automatic env loading only applies to the app runtime, not arbitrary scripts).
  </action>
  <acceptance_criteria>
    - `scripts/seed.ts` contains `process.env.SUPABASE_SERVICE_ROLE_KEY` and does not hardcode any key value
    - `scripts/seed.ts` contains at least 3 distinct `slug` string literals
    - `scripts/seed.ts` contains `.upsert(` with `onConflict: 'slug'`
    - `scripts/seed.ts` contains `storage.from('package-photos').upload(`
    - `package.json` `scripts.seed` runs `tsx` against `scripts/seed.ts`
    - `grep -rl "SUPABASE_SERVICE_ROLE_KEY" app/ components/` returns no matches (key never imported into app code)
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit scripts/seed.ts 2>&1 | tail -30</automated>
  </verify>
  <done>Seed script authored, type-checks cleanly, is idempotent via upsert-on-slug + delete/reinsert children, and never leaks the service-role key into app-bundled code.</done>
</task>

<task type="auto">
  <name>Task 2: Run seed script and verify live data</name>
  <files>scripts/seed.ts</files>
  <read_first>
    - scripts/seed.ts (the file Task 1 just wrote)
  </read_first>
  <action>
    Run `npm run seed` against the live Supabase project linked in `01-02`. Confirm it completes without throwing.
    Run it a second time immediately to prove idempotency — the package row count must not change between the two runs (upsert-on-slug + delete/reinsert children means re-running produces the same 3 packages, not 6).
    Verify row counts via the Supabase CLI or a one-off query: `packages` has exactly 3 rows, `package_photos` has at least 6 rows, `itinerary_days` has at least 9 rows, `package_inclusions` has at least 24 rows (3 packages x 8 minimum items), `faq_facts` has exactly 3 rows. Verify the Storage bucket `package-photos` contains at least 6 objects.
  </action>
  <acceptance_criteria>
    - Two consecutive `npm run seed` runs leave `packages` at exactly 3 rows (no duplication)
    - Every one of the 5 tables has a nonzero row count after seeding
    - `package-photos` Storage bucket has at least 6 objects, each fetchable at its public URL (HTTP 200, `image/jpeg` content-type)
  </acceptance_criteria>
  <verify>
    <automated>npm run seed && npm run seed 2>&1 | tail -20</automated>
  </verify>
  <done>Live Supabase project holds 3 fully-populated placeholder packages with real Storage-hosted photos; re-running the seed script is a safe no-op on row count.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI operator shell → Supabase service-role key | Seed script runs with full RLS-bypassing privileges, must never run in a browser/user-facing context |
| Local seed-asset JPGs → public Storage bucket | Files committed to the repo become publicly fetchable once uploaded |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-06 | Information Disclosure | `scripts/seed.ts` service-role key usage | high | mitigate | Key read exclusively from `process.env.SUPABASE_SERVICE_ROLE_KEY` via `.env.local` (gitignored); script lives under `scripts/`, outside the Next.js bundle graph; acceptance criteria greps `app/`/`components/` to confirm no accidental import |
| T-01-07 | Tampering / Denial of Service | Seed re-run duplicating or corrupting rows | low | accept | Upsert-on-slug + delete/reinsert-children design (per RESEARCH Open Question #1) makes re-runs idempotent by construction; this is disposable dev tooling per D-01, not a production write path — low blast radius even if it misbehaves |
| T-01-08 | Information Disclosure | Placeholder photos publicly readable in Storage | low | accept | Intentional — the entire public catalog is meant to be publicly browsable; no PII or sensitive content in placeholder travel photos |

</threat_model>

<verification>
`npm run seed` run twice in a row leaves `packages` at exactly 3 rows. All 5 tables and the Storage bucket are populated. A public Storage URL for one seeded photo returns HTTP 200 when fetched directly.
</verification>

<success_criteria>
- 3 placeholder PH tour packages live in Supabase with complete itinerary/inclusions/faq/photo data
- Seed script is safely re-runnable with no row duplication
- No service-role key leakage into app-bundled code
</success_criteria>

<output>
Create `.planning/phases/01-public-catalog-inquiry-entry-point/01-03-SUMMARY.md` when done
</output>

## Artifacts this phase produces

- `scripts/seed.ts` — idempotent seed script (`npm run seed`), upserts 3 placeholder packages with photos/itinerary/inclusions/faq_facts
- `supabase/seed-assets/*.jpg` — committed placeholder photo source files
